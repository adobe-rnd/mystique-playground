{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "hero",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "const": "hero"
    },
    "heading": {
      "type": "string",
      "description": "The main heading of the hero block."
    },
    "subheading": {
      "type": "string",
      "description": "The subheading or description text."
    },
    "image": {
      "$ref": "image.json",
      "description": "The image for the hero block."
    },
    "buttons": {
      "type": "array",
      "description": "An array of buttons that can be used as CTAs.",
      "items": {
        "$ref": "button.json"
      }
    },
    "imagePosition": {
      "type": "string",
      "enum": ["left", "center", "right"],
      "description": "The position of the image relative to the content."
    },
    "imageOverlay": {
      "type": "boolean",
      "description": "Whether the image should have an overlay."
    },
    "alignment": {
      "type": "string",
      "enum": ["left", "center", "right"],
      "description": "The text alignment for the content."
    },
    "layout": {
      "type": "string",
      "enum": ["stacked", "inline"],
      "description": "Layout type for the content relative to the image."
    }
  },
  "required": ["kind", "heading", "subheading", "image", "buttons", "imagePosition", "imageOverlay", "alignment", "layout"],
  "additionalProperties": false
}
